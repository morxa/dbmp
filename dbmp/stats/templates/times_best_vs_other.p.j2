{% extends "common.p.j2" %}
{% block data %}
set xlabel "{{ other_planner }} Planning Time (s)"
set ylabel "Planning Time (s)"
set key top left
set logscale
plot \
  "{{ other_data_file }}" title "{{ other_planner }}" ls 1, \
  "{{ best_data_file }}" title "DBMP ({{ evaluator }})" ls 2
{% endblock %}
